import {
  replace,
  replaceDOM,
  replaceDOMElements
} from './replacers';

import {
  containsEmoji,
  isEmoji
} from './utils';

export default {
  containsEmoji,
  isEmoji,
  replace,
  replaceDOM,
  replaceDOMElements
};
